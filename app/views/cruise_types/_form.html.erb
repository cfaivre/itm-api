<%= form_for(@cruise_type) do |f| %>
  <% if @cruise_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cruise_type.errors.count, "error") %> prohibited this cruise_type from being saved:</h2>

      <ul>
      <% @cruise_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
